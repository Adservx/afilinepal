services:
  - type: web
    name: ecommerce-admin
    env: node
    buildCommand: npm ci && npx puppeteer browsers install chrome && npm run build:react
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERVER_PORT
        value: 10000